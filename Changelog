Mon Mar 17th 2026 - bjiyoung - v5.1.4.6
    - BUGFIX: Auto-confirm timer now stops on any user interaction in Add Torrent
      dialog (replaced individual signal connects with eventFilter) (bjiyoung)
    - BUGFIX: Fixed "Completed" status showing before file move to save path.
      When both save path and download path are set, m_hasFinishedStatus is now
      set to true before adjustStorageLocation() so the target is correctly
      savePath() instead of downloadPath() (bjiyoung)

